# ASM

Internal Vision 360 utilities.

## Machine Health compilation

Verified Machine-1 / ideal / B2B mini 5.0 EDF data sheet (sample fixtures; live `D:\\demo` files were not on the build VM): [`machine-health-compilation/`](machine-health-compilation/).

## Diamond Utility

Desktop-style local diamond data processor (scan → select → Get MP4).

```bash
cd diamond-utility
npm install
npm run dev
```
